name="audio"
description="Audio Control - Volume, playback, and device management"
version="0.1.0"
method="stow"
collection="system"
fold=false

# Ignore documentation files
supported_os=("linux")
depends=()
requires_commands=("pactl")
ignore=(
    "README.md"
)
